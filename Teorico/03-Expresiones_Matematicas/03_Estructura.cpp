#include <iostream>
#include <cmath> // Para hacer uso de algunas expresiones matemáticas puntuales, facilita hacer uso de la biblioteca cmath
using namespace std;

int main()
{
    // ❗ Supongamos que se ingresan valores con muchos decimales. precision() permite acotar la cantidad de decimales. Si buscas una precision de 2, tomará 2 decimales y los redondeará a uno.
    cout.precision(3);

    // sqrt(x): Raíz cuadrada de x.
    // pow(base, exp): Calcula base elevado a exp.
    // abs(x): Valor absoluto de x.
    // ceil(x): Redondeo hacia arriba.
    // floor(x): Redondeo hacia abajo.
    // exp(x): Exponencial (e^x).
    // log(x): Logaritmo natural (base e).
    // log10(x): Logaritmo en base 10.
    // sin(x): Seno de x (en radianes). 
    
    return 0;
}