#include <iostream>
using namespace std;

int main()
{
    // Un puntero es, prácticamente, una variable que almacena la dirección de memoria de otra variable. Esto permite una mayor flexibilidad en el manejo de datos, especialmente en estructuras dinámicas como arreglos y listas enlazadas.

    /* Características de los punteros:
       - Los punteros pueden apuntar a cualquier tipo de dato (int, float, char, etc.)
       - Se utilizan para la manipulación directa de la memoria
       - Permiten la creación de estructuras de datos dinámicas
       - Facilitan la gestión de memoria y el paso de grandes estructuras a funciones sin copiar los datos
    */

    // Declaración de un puntero: tipoDeDato *nombrePuntero = &varible;

    // Para acceder al contenido almacenado en la dirección de memoria: *nombrePuntero;
    // Para acceder a la dirección de memoria: nombrePuntero;

    return 0;  